require 'rubygems'
require 'colorize'

task :install do
  puts 'Installing dependencies'.green
  system 'bower install'
  system 'npm install'
  puts 'Done'.green
end

namespace :aws do
  task :update_regions do
  end

  task :closest_region do
  end
end
